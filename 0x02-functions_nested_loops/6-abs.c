#include "main.h"
#include <stdlib.h>

/**
 * _abs - get the absolute value of integer
 * @n: number to be computed
 *
 * Return: computed value
 */

int _abs(int n)
{
	int computed = abs(n);

	return (computed);
}
