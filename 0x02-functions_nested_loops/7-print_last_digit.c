#include "main.h"

/**
 * print_last_digit -  gets the last digits in the number
 *
 * @n: number
 * Return: the last digit
 */

int print_last_digit(int n)
{
	int lastDigit = n % 10;
	return (lastDigit);
}
